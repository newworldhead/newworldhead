export default function SectionPrimary({ children }) {
    return (
        <section>
            <div className="container mx-auto my-4">
                {children}
            </div>
        </section>
    )
}
