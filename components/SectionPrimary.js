export default function SectionPrimary({ children }) {
    return (
        <section>
            <div className="container mx-auto my-10">
                {children}
            </div>
        </section>
    )
}
