import Link from 'next/link'

export default function Company({ company }) {
    const { name, recruiting, slug, fraction_image } = company
    return (
        <li className="border-gray-400 flex flex-row mb-2">


        </li>
    )
}
